EXECUCAO:
pip install pymongo flask certifi
python -m flask run

CRITERIO DE ORDENACAO:
o criterio de ordenacao foi a quantidade de vezes que a keyword procurada foi encontrada na descricao e titulo do manga


o arquivo keywords.py foi utilizado para gerar a lista invertida e salvar eles num mongodb